const SearchBar = () => (
    <input
        type="text"
        placeholder="Search..."
        className="f6 pointer flex items-center"
        style={{
            borderRadius: '0.4rem',
            marginBottom: '1rem'
        }}
    />
)

export default SearchBar;